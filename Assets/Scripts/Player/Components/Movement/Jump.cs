using UnityEngine;

public class Jump : MonoBehaviour
{
    /*public void Jump()
    {
        if (!player.IsGrounded) return;

        velocity.y = 0;

        // Calculate the velocity required to achieve the target jump height.
        velocity.y = Mathf.Sqrt(2 * PlayerData.initialJumpHeight * Mathf.Abs(Physics2D.gravity.y));

        PlayerTransform.Translate(velocity * Time.deltaTime);
    }*/
}
